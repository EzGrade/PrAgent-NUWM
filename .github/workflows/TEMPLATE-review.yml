# 📋 TEMPLATE: PR Agent Workflow для репозиторіїв студентів
# 
# Інструкція:
# 1. Скопіюйте цей файл до вашого репозиторію як `.github/workflows/review.yml`
# 2. Переконайтеся, що в Settings → Secrets and variables → Actions додані всі необхідні секрети
# 3. Коли створюєте Pull Request, бот автоматично перевірить ваш код
#
# Необхідні секрети (налаштовуються адміністратором):
# - APP_ID
# - PRIVATE_KEY
# - INSTALLATION_ID (опціонально)
# - OPENAI_API_KEY
# - OPENAI_MODEL
# - GOOGLE_CREDENTIALS_CONTENT
# - SPREADSHEET_URL
# - SHEETS_NAMING

name: PR Agent for NUWM

on:
  pull_request:
    types: [ opened, edited, synchronize, reopened ]

jobs:
  pr_agent_job:
    runs-on: ubuntu-latest
    name: Run pr agent on every pull request
    steps:
      - name: pr-agent-nuwm
        uses: EzGrade/Pr-Agent-NUWM@main
        env:
          GIT_APP_ID: ${{ secrets.GIT_APP_ID }}
          GIT_PRIVATE_KEY: ${{ secrets.GIT_PRIVATE_KEY }}
          GIT_INSTALLATION_ID: ${{ secrets.GIT_INSTALLATION_ID }}
          GIT_REPOSITORY: ${{ github.repository }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          GOOGLE_CREDENTIALS_CONTENT: ${{ secrets.GOOGLE_CREDENTIALS_CONTENT }}
          GOOGLE_SPREADSHEET_URL: ${{ secrets.GOOGLE_SPREADSHEET_URL }}
          GOOGLE_SHEETS_NAMING: ${{ secrets.GOOGLE_SHEETS_NAMING }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
